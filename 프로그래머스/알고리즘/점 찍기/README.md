
## 문제링크
[점 찍기](https://school.programmers.co.kr/learn/courses/30/lessons/140107)
## 첫번째 시도

```
import math

def solution(k, d):
    answer = 0
    for i in range(0, d+1, k):
        for j in range(0, d+1, k):
            if math.sqrt(i**2+j**2)<=d:
                answer+=1
    return answer
```
## **🚫문제점**


제공하신 코드는 2차원 좌표 평면에서 원점 $(0, 0)$으로부터의 거리가 $d$ 이하인 점 $(i, j)$의 개수를 찾는 문제입니다. 여기서 $i$와 $j$는 $k$의 배수여야 합니다.

현재 코드는 **이중 `for` 루프**를 사용하고 있으며, $d$가 커지면 $O((d/k)^2)$의 시간 복잡도를 가져 **시간 초과(Time Limit Exceeded)**가 발생할 가능성이 매우 높습니다.

시간 초과를 줄이는 가장 좋은 방법은 **내부 `for` 루프를 수학적 계산으로 대체**하여 **$O(d/k)$**의 시간 복잡도로 줄이는 것입니다.

----------

## 🚀 시간 초과를 줄이는 방법 (내부 루프 제거)

### 1. 문제 분석 및 수학적 변환

우리는 다음 조건을 만족하는 정수 $i, j$의 쌍의 개수를 찾아야 합니다:

1.  $i = a \cdot k$, $j = b \cdot k$ (단, $a, b$는 정수)
    
2.  $0 \le i \le d$ 이고 $0 \le j \le d$
    
3.  $\sqrt{i^2 + j^2} \le d$
    

세 번째 부등식을 제곱하면 $\mathbf{i^2 + j^2 \le d^2}$ 이 됩니다.

### 2. 내부 루프 제거 전략

현재 코드는 바깥 루프에서 $i$를 고정하고, 안쪽 루프에서 **가능한 모든 $j$를 시도**합니다.

수학적 조건을 이용하면, **고정된 $i$**에 대해 $j$가 만족해야 할 최대 값은 다음과 같습니다.

$$\sqrt{i^2 + j^2} \le d$$

$$i^2 + j^2 \le d^2$$

$$j^2 \le d^2 - i^2$$

$$j \le \sqrt{d^2 - i^2}$$

따라서, 바깥 루프에서 $i$ 값이 결정되면, $j$가 가질 수 있는 최댓값 $J_{max}$는 $\mathbf{\lfloor \sqrt{d^2 - i^2} \rfloor}$이 됩니다.

$j$는 $0$부터 $J_{max}$까지 $k$의 배수여야 하므로, 가능한 $j$의 개수 (즉, **만족하는 점의 개수**)는 다음과 같이 한 번의 계산으로 구할 수 있습니다.

$$\text{개수} = \left\lfloor \frac{J_{max}}{k} \right\rfloor + 1$$

> `+1`을 하는 이유는 $0 \cdot k$ (즉, $j=0$)도 포함되기 때문입니다.

## 💡수정된 코드


이중 루프 중 안쪽 루프를 제거하고 수학적 계산으로 대체합니다.
```
import math

def solution(k, d):
    answer = 0
    
    # 1. i를 0부터 d까지 k 간격으로 순회 (O(d/k) 시간 복잡도)
    for i in range(0, d + 1, k):
        
        # 2. 고정된 i에 대해 j가 가질 수 있는 최대 유효 거리 J_max를 계산
        # j^2 <= d^2 - i^2
        # J_max = sqrt(d^2 - i^2)
        
        # math.sqrt() 사용을 위해 d^2 - i^2는 반드시 0 이상입니다.
        j_max_float = math.sqrt(d**2 - i**2)
        
        # 3. j_max를 정수형으로 변환 (내림, 즉 ⌊⌋)
        j_max = int(j_max_float)
        
        # 4. 0부터 j_max까지 k의 배수인 점의 개수를 계산
        # (J_max / k)의 몫에, 0을 포함하기 위해 +1을 더합니다.
        
        # Python의 // 연산자는 정수 몫을 계산하므로 ⌊⌋와 같습니다.
        count = (j_max // k) + 1
        
        answer += count
        
    return answer


```

## 분석
### 로직
아이디어는 좋았는데 시간복잡도에서 걸렸음
max값을 계산해서 루프를 한개 줄이는 것으로 해결할 수 있다고 함
시험장에서 이런거 마주치면 걍 테케 긁고 다른 문제 푸는게 나을듯

### 문법
math.sqrt로 제곱근 계산이 가능하다. 제곱은 그냥 **연산자 쓰면 됨.
```
import math
sq=math.sqrt(num)
```
> Written with [StackEdit](https://stackedit.io/).
