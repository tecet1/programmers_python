## 문제링크
[숫자 짝](https://school.programmers.co.kr/learn/courses/30/lessons/131128)
## 첫번째 시도
```
def solution(X, Y):
    answer = ''
    X_dict={0 for _ in range(10)}
    Y_dict={0 for _ in range(10)}
    str_X = str(X)
    str_Y = str(Y)
    
    for character in str_X:
        key_char = int(character)
        X_dict[key_char] += 1
        
    for character in str_Y:
        key_char = int(character)
        Y_dict[key_char] += 1
        
    for i in range(10):
        if X_dict[i] > 0 and Y_dict[i] > 0:
            temp = min(X_dict[i], Y_dict[i])
            char = str(i)
            for _ in range(temp):
                answer.insert(char)
        
    return answer
```
위 코드는 내가 생각한 이 문제의 해결방법을 작성한 것으로, 
의도는 딕셔너리를 만들어서 X,Y 각각의 자릿수의 빈도를 저장하고
그렇게 완성된 딕셔너리의 value값을 통해 반환할 문자열의 앞에 자릿수를 반복해서 붙여넣으려 했다.
하지만 파이썬 숙련도 이슈로 인해 자료형 오류를 마주했고 ai한테 올바른 문법 사용방법을 물어봤다.

----------

## 🛑 코드의 주요 문제점 분석

### 1. ❌ 딕셔너리 초기화 오류 (가장 치명적인 문제)

현재 `X_dict`와 `Y_dict`를 딕셔너리가 아닌 **리스트 컴프리헨션**으로 잘못 초기화했습니다. 게다가 파이썬 딕셔너리의 초기화 구문도 아닙니다.

Python

```
X_dict={0 for _ in range(10)}
Y_dict={0 for _ in range(10)}
```

이 코드는 실제로는 `{0}`이라는 **단일 원소를 가진 Set**을 만듭니다. Set은 키-값 쌍을 저장하는 딕셔너리처럼 작동하지 않으며, 단일 값만 포함하므로 숫자의 빈도를 셀 수 없습니다.

**올바른 초기화:** 빈도를 저장하려면 **키를 0부터 9까지의 숫자(문자열 혹은 정수)**로, **값을 빈도(0)**로 가지는 딕셔너리 또는 리스트(인덱스를 이용)를 사용해야 합니다.

### 2. ❌ 문자열 추가 메서드 오류

파이썬의 문자열(String)에는 `insert()`라는 메서드가 없습니다. 문자열에 문자를 추가하려면 **`+` 연산자**를 사용하거나, 리스트에 모은 후 `join()`을 사용해야 합니다.

Python

```
# 오류 라인
answer.insert(char)
```

### 3. ❌ 가장 큰 수를 만들기 위한 정렬 로직 부족

`answer` 문자열에 공통 숫자를 추가할 때, 현재 코드는 **숫자 0부터 9까지 순서대로** 빈도를 확인하며 추가합니다.

Python

```
# 현재 추가 순서: 0 -> 1 -> 2 -> ... -> 9
for i in range(10): 
    # ... answer에 str(i) 추가
```

가장 큰 정수를 만들려면 **가장 큰 숫자(9)부터 가장 작은 숫자(0) 순서**로 추가해야 합니다.

----------

## ✅ 수정된 올바른 코드

두 정수 $X, Y$에서 공통으로 나타나는 숫자를 이용해 **가장 큰 정수**를 반환하는 로직으로 수정했습니다.

Python

```
from collections import Counter

def solution(X, Y):
    # 1. 문자열로 변환 후 각 숫자의 빈도를 계산 (Counter 사용)
    # Counter는 딕셔너리 형태로 {숫자: 빈도수}를 자동으로 계산해 줍니다.
    count_X = Counter(str(X))
    count_Y = Counter(str(Y))
    
    answer_chars = [] # 공통 숫자를 저장할 리스트

    # 2. 9부터 0까지 역순으로 순회 (가장 큰 수를 만들기 위함)
    for i in range(9, -1, -1):
        char = str(i)
        
        # 3. 두 카운터에 해당 숫자가 존재하는지 확인
        if char in count_X and char in count_Y:
            
            # 4. 두 수 중 더 적게 나타난 빈도수를 찾습니다.
            min_count = min(count_X[char], count_Y[char])
            
            # 5. 해당 숫자를 그 횟수만큼 리스트에 추가합니다.
            answer_chars.extend([char] * min_count)

    # 6. 결과 처리
    
    # 짝꿍이 없는 경우
    if not answer_chars:
        return "-1"
    
    # 공통 숫자가 0밖에 없는 경우 (예: X='100', Y='20') -> '0'을 반환
    if answer_chars[0] == '0':
        return "0"

    # 짝꿍 숫자를 모두 모았으면, 리스트를 문자열로 합쳐서 반환합니다.
    return "".join(answer_chars)


```

### 💡 수정 요약

1.  **빈도 계산:** `collections.Counter`를 사용하여 빈도수 계산을 간결하고 정확하게 수정했습니다.
    
2.  **반복문 순서:** `range(9, -1, -1)`를 사용하여 **9부터 0까지 역순**으로 확인하여 가장 큰 수를 보장했습니다.
    
3.  **문자열 추가:** `answer.insert()` 대신 `answer_chars.extend([char] * min_count)`로 리스트에 숫자를 모은 후, 최종적으로 `"".join(answer_chars)`를 사용하여 효율적으로 문자열을 만들었습니다.
    
4.  **예외 처리:** 문제의 요구사항에 따라 짝꿍이 없는 경우 `"-1"`, 짝꿍이 `0`으로만 이루어진 경우 `"0"`을 반환하는 로직을 추가했습니다.

위 내용은 counter라는 생소한 모듈을 import해서 푼 것 같은데
일단 내가 생각했던 로직을 올바른 문법으로 구현해달라고 요청했다. 

## 💡수정된 코드
```
def solution(X, Y):
    
    # 1. 딕셔너리 초기화 (0부터 9까지 키를 가지고 값은 0으로 시작)
    # 일반적인 딕셔너리 초기화 방법입니다.
    X_dict = {i: 0 for i in range(10)}
    Y_dict = {i: 0 for i in range(10)}
    
    # 정수를 문자열로 변환
    str_X = str(X)
    str_Y = str(Y)
    
    # 2. X의 숫자 빈도 계산
    for character in str_X:
        key_int = int(character)
        X_dict[key_int] += 1
            
    # 3. Y의 숫자 빈도 계산
    for character in str_Y:
        key_int = int(character)
        Y_dict[key_int] += 1
            
    # 4. 짝꿍 숫자를 모아서 가장 큰 정수를 만듦
    answer_chars = []
    
    # 가장 큰 수를 만들기 위해 9부터 0까지 역순으로 순회
    for i in range(9, -1, -1):
        # 딕셔너리의 키는 정수이므로, 정수 i를 사용합니다.
        
        # X와 Y 모두 i를 포함하는 경우
        if X_dict[i] > 0 and Y_dict[i] > 0:
            
            # 공통으로 사용 가능한 횟수 (더 적은 빈도)
            count = min(X_dict[i], Y_dict[i])
            
            # i를 문자열로 변환하여 횟수만큼 answer_chars에 추가
            char = str(i)
            answer_chars.extend([char] * count)

    # 5. 결과 처리 및 반환
    
    # 짝꿍이 없는 경우
    if not answer_chars:
        return "-1"
    
    # 짝꿍이 '0'으로만 이루어진 경우 (예: "000" -> "0" 반환)
    if answer_chars[0] == '0':
        return "0"

    # 최종 결과 문자열로 합쳐서 반환
    return "".join(answer_chars)
```

## 분석
### 로직
해당 사항 없음.

### 문법
#### 1. 딕셔너리
딕셔너리 초기화를 다음과 같은 방법으로 할 수 있음. 
```
X_dict = {i: 0 for i in range(10)}
Y_dict = {i: 0 for i in range(10)}
```
내가 처음에 작성한 코드는 set의 초기화에 해당함.
```
X_dict={0 for _ in range(10)}
Y_dict={0 for _ in range(10)}
```
딕셔너리의 기본 구조가 {key : value} 이기에 key:value를 사용해야 정상적인 딕셔너리를 생성할 수 있는 것이다.

#### 2. extend, join
**extend**: 같은 element를 여러번 반복해서 리스트 뒤에 붙이는 메서드.
```
answer_chars.extend([char] * count)
```
**join**: 리스트를 문자열로 바꾸는 메서드
```
return "".join(answer_chars)
```

#### 3. counter
빈도수를 자동으로 계산해서 할당하는 collection 라이브러리의 딕셔너리라고 한다.
```
from collections import Counter

def solution(X, Y):
    # 1. 문자열로 변환 후 각 숫자의 빈도를 계산 (Counter 사용)
    # Counter는 딕셔너리 형태로 {숫자: 빈도수}를 자동으로 계산해 줍니다.
    count_X = Counter(str(X))
    count_Y = Counter(str(Y))
```
딕셔너리 직접 초기화하고 계산하고 하는 과정을 딸깍으로 스킵할 수 있음.
굉장히 파워풀하다는 것을 알 수 있었고 빈도수를 계산하는 문제라면 적극적으로 활용해야겠음.        
> Written with [StackEdit](https://stackedit.io/).